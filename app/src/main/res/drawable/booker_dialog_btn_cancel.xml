<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:background="@color/dialog_confirm_btn_sure_bg" /><!-- 填充的颜色 -->
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:topLeftRadius="@dimen/size_26"
        android:topRightRadius="@dimen/size_26"
        android:bottomRightRadius="@dimen/size_26"
        android:bottomLeftRadius="@dimen/size_26"/>
    <!-- 边框粗细及颜色 -->
    <stroke android:width="1dp" android:color="#E6E6E6" />
</shape>